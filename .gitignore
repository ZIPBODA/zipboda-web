# dependencies
node_modules/

# env & secrets  (실제 토큰/키는 절대 커밋 금지)
.env
.env.*
!.env.example

# Claude Code 로컬(개인) 설정 — 팀 공유는 .claude/settings.json 사용
.claude/settings.local.json

# build/output
dist/
build/
coverage/
.next/
.expo/
*.log
*.tsbuildinfo

# OS/IDE
.DS_Store
Thumbs.db

# 로컬 작업 폴더 — PDF 도구 설치본과 중간 산출물이 쌓인다
/tmp/
