/**
 * shared/ui — 공용 UI 진입점.
 * 공유 디자인시스템(@zipboda/ui, React DOM)을 재노출한다.
 * 인라인 재구현 금지(frontend-rule R10 / figma-implementation-rule D4).
 * web 전용 래퍼/조합이 필요하면 이 레이어에 추가한다.
 */
export * from "@zipboda/ui";
